//akka configuration common override. Works like you would expect it to. ONLY PUT TEST SETTINGS HERE
akka {
  loglevel = "DEBUG"
  actor {
    serialize-messages = on
    serialize-creators = on
  }
}